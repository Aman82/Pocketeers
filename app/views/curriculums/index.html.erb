<div class="row">
<div class="large-12 medium-8 columns">
<div class="c-header"></div>
<h2>Teach a course from our Prebuilt Curriculums</h2>
<h3 class="subheader">Guided Learning tracks built by industry professionals</h3>

<hr />
</div>
</div>


<div class="row">
<h5 class="subheader">Select topic to view dropdown menu</h5>
   <div class="large-12 columns">
      <dl class="accordion " data-accordion>
         <% @curriculums.each do |c| %>
         <dd class="accordion-navigation dropdown">
            <a href='#panel<%= c.id %>'>+  <%= c.title %><p id="creator">Created by: <%= c.user.full_name %></p></a>
            <div id='panel<%= c.id %>' class="content curriclumInfo">
            <ul class="dropdown">
               <li><strong><%= c.title %></strong></li>
               <li><%= c.topic %></li><br>
               <li><%= c.summary %></li>
               <li><%= truncate(c.syllabus, length: 300, separator: '') %></li>
               <li><%= link_to "More Info on Framework", c %></li><br>
               <li><strong>Course Length: </strong><%= c.timeframe %></li>
               
               <li><%= link_to "Create new Group from Curriculum", new_group_path(:id => c.id) %></li>
               </ul>
               <br/>
            </div>
         </dd>
         <% end %>
      </dl>
     
      <div class="large-4 small-3 columns">
            <%= link_to 'Create your own curriculum! +', new_curriculum_path, class:"tiny button expand" %>
        </div>
   </div>
    <h5 class="subheader" style="text-align: center;">Today is a very exciting day</h5>
</div>

